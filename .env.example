# This application runs entirely in the browser and doesn't require any environment variables.
# All data processing happens client-side with no backend or database.

# No environment variables needed!
